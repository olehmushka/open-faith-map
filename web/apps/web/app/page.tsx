export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-4 px-6">
      <h1 className="text-2xl font-semibold">OpenFaithMap</h1>
      <p>
        This is a placeholder for <code>openfaithmap-web</code> — the public site, congregation-admin
        console, and moderator console described in{" "}
        <code>docs/modules/web-facade.md</code>. No real UI exists yet; see the repository{" "}
        <code>README.md</code> for the current build stage.
      </p>
    </main>
  );
}
