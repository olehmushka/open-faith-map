# Documents the shape openfaithmap-web reads its config from. Currently empty — this app never
# holds a session, so it has no AUTH_* vars either (M2.1, D-AdminSurface). M4's discovery pages read
# OPENFAITHMAP_API_BASE_URL, but only docker-compose.yml's openfaithmap-web service sets it today
# (a host-run `next dev` here has no committed example value yet, same gap this file's admin sibling
# doesn't have) — no oikumenea-facing variable of any kind has ever belonged here (D-AdminSurface:
# this app has no credential to forward, so it never talks to go-oikumenea directly, before or after
# M10's core absorption).
