# Documents the shape openfaithmap-web reads its config from. Currently empty — the public site
# serves only a static placeholder home page and reads no environment variables at all (M2.1,
# D-AdminSurface: this app never holds a session, so it has no AUTH_* vars either). Add
# OIKUMENEA_BASE_URL / OPENFAITHMAP_API_BASE_URL here once a real data-reading public page exists
# (M4, docs/modules/discovery.md) — not scaffolded now for a page that doesn't exist yet.
